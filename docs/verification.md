# OMIGYM 验收记录

日期：2026-09-22。使用本机静态服务和 Codex 浏览器，未发布网站。

## 自动检查

- `npm test`：5 项通过。覆盖相同规格合并、不同规格分离、20 件上限与移除、未知商品/规格拒绝、损坏的存储内容、整数分金额、运费门槛、订单快照及空购物车拒绝。
- `npm run check`：全部 JavaScript 语法通过；17 种页面渲染、9 项本地引用资源、6 条商品记录通过；前端无外部资源或网络提交代码。
- 本地入口 HTTP 200。

## 浏览器实际操作

- 首页、商品列表、详情、关于、联系、购物车、结算、订单确认可访问；未知商品显示恢复入口。
- 分类选择 Racks & benches 得到两款商品，高价优先排序为训练架、训练凳。
- 无匹配搜索展示空结果，清除筛选可恢复六款商品。
- 哑铃选择 5 kg，价格由 $89 更新为 $69；图库切换显示用户提供的 5 kg 实物图。
- 加购、增加、减少、删除、空购物车、弹层关闭、刷新恢复购物车均已验证。
- 5 kg 哑铃两对：小计 $138，运费 $14.95，合计 $152.95；三对合计 $207 且免运费。
- 填写虚构配送资料完成模拟下单。未勾选演示确认时无法提交；成功后显示订单号、两对哑铃、$152.95 合计，购物车归零。
- 确认页刷新后订单号保留。空购物车进入结算不会显示可提交订单的表单。
- 联系表单填写虚构资料后展示本地成功状态，不发送邮件。
- WebMCP 工具注册成功；无效规格被拒绝，有效 7.5 kg 加购返回 1 件/$89.00，并同步到可见购物车。
- 浏览器错误和警告日志为空。

## 响应式和视觉

- 检查桌面首页、商品详情；手机首页、导航、商品列表、详情、购物车弹层和结算。
- 390px 视口检查商品和结算，无页面横向溢出。
- 最窄测试中浏览器报告实际 innerWidth 291px、内容宽 277px，首页、列表、详情的 scrollWidth 均为 277px，无横向溢出。
- 响应式测试结束后已恢复正常浏览器尺寸并保留首页预览。
- 五张生成图及两张嵌入的用户实物图均目视检查，无其他品牌标识。第三张原始参考图包含杂物标签，未用于网页。

## 审查发现与修复

独立代码审查发现两处问题，均已修复并在浏览器复测：

1. 购物车弹层编辑后，底层购物车/结算金额陈旧。先复现数量已为 3、页面仍显示 $152.95；修复后同步显示 $207。退回 2 件更新为 $152.95，结算填写的名字仍保留。
2. 购物车页面数量变更后键盘焦点丢失。修复后焦点保留在对应的增加/减少按钮，实际读取 activeElement 验证。

资源检查还发现空购物车结算缺少 H1，已补充并通过检查。窄屏导航曾存在横向溢出，已调整并复测。

## 边界

这是演示商城；无真实库存、交易、支付、物流或邮件发送。产品参数和评分为示例。参考网站本次无法通过证书验证，未复制其内容。自动化测试集中于核心购物车规则；UI 流程通过实际浏览器操作验证，未添加第三方测试依赖。
